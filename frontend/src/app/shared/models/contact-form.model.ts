export interface ContactFormModel {
  name: string;
  contactInfo: string;
  subject: string;
  message: string;
}

export interface ContactFormErrors {
  name: string;
  contactInfo: string;
  subject: string;
  message: string;
}
